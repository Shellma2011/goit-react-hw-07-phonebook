import styled from 'styled-components';

const ContactListStyled = styled.ul`
  display: flex;
  align-items: flex-start;
  flex-direction: column;

  width: 280px;
  margin: 20px auto;

  padding: 12px 16px;
  border: 1px solid #2a2a2a;
  border-radius: 4px;

  font: inherit;
  line-height: 16px;
  letter-spacing: 0.01em;
`;

export { ContactListStyled };
